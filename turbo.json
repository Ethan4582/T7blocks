{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "out/**"],
      "env": ["NODE_VERSION"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {}
  }
}